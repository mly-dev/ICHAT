/**
 * Point d'entrée unique vers ce qui ne m'appartient pas encore.
 * Remplacer ces ré-exports par le vrai code d'Adam le moment venu.
 */
export { useAuth, getAuthSnapshot, getCurrentUserId } from './authStore';
export { searchDirectory, getDirectoryUser } from './directory';
